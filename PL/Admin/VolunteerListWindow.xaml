<Window x:Class="PL.Admin.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Volunteer List Window" 
        Height="800" 
        Width="1000"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ComboBox להבחנה בין המתנדבים -->
        <ComboBox Grid.Row="0"
                  SelectedItem="{Binding SelectedCallType, Mode=TwoWay}" 
                  ItemsSource="{Binding CallTypes}" 
                  
                  VerticalAlignment="Center" 
                  Margin="0,10" >
            <ComboBoxItem Content="PreparingFood" Tag="PreparingFood" />
            <ComboBoxItem Content="TransportingFood" Tag="TransportingFood" />
            <ComboBoxItem Content="FixingEquipment" Tag="FixingEquipment" />
            <ComboBoxItem Content="ProvidingShelter" Tag="ProvidingShelter" />
            <ComboBoxItem Content="TransportAssistance" Tag="TransportAssistance" />
            <ComboBoxItem Content="MedicalAssistance" Tag="MedicalAssistance" />
            <ComboBoxItem Content="EmotionalSupport" Tag="EmotionalSupport" />
            <ComboBoxItem Content="PackingSupplies" Tag="PackingSupplies" />
            HorizontalAlignment="Stretch"
        </ComboBox>

        <!-- ListView להצגת המתנדבים -->
        <ListView Grid.Row="1"
                  Margin="0,10"
                  ItemsSource="{Binding VolunteerList}" 
                  SelectedItem="{Binding SelectedVolunteer}" 
                  MouseDoubleClick="lsvVolunteerList_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Id}" Width="100" />
                    <GridViewColumn Header="Full Name" DisplayMemberBinding="{Binding FullName}" Width="200" />
                    <GridViewColumn Header="Is Active" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive}" IsEnabled="False" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Treated Calls" DisplayMemberBinding="{Binding SumTreatedCalls}" Width="150" />
                    <GridViewColumn Header="Canceled Calls" DisplayMemberBinding="{Binding SumCanceledCalls}" Width="150" />
                    <GridViewColumn Header="Expired Calls" DisplayMemberBinding="{Binding SumExpiredCalls}" Width="150" />
                    <GridViewColumn Header="Call in Treatment" DisplayMemberBinding="{Binding CallIdInTreatment}" Width="150" />
                    <!-- Add the "Delete" button column -->
                    <GridViewColumn Header="Actions"  >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <!-- Delete Button for each row -->
                                <Button Content="Delete" Click="DeleteButton_Click" Tag="{Binding Id}" Foreground="#009990"  />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                </GridView>
            </ListView.View>
        </ListView>

        <!-- כפתור הוספה -->
        <Button Grid.Row="2"
                Content="Add"
                HorizontalAlignment="Center"
                Width="150"
                Margin="0,10,0,10"
                Command="{Binding btnAddUpdate_Click}" 
                Click="ButtonAdd_Click" 
                Background="#4CAF50" 
                Foreground="White" 
                FontWeight="Bold" 
                BorderThickness="0"
                Cursor="Hand" />
    </Grid>
</Window>
