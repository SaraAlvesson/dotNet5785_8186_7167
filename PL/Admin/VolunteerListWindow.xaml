<Window x:Class="PL.Admin.VolunteerListWindow"
     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
     xmlns:local="clr-namespace:PL"
     mc:Ignorable="d"
     Title="VolunteerListWindow" Height="640" Width="800"
     DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Window.Resources>
        <local:VolunteerCollection x:Key="VolunteerCollectionKey" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*" />
            <RowDefinition Height="500*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ComboBox ItemsSource="{Binding Source={StaticResource VolunteerCollectionKey}}"
          Grid.Column="1"
          SelectedValue="{Binding SelectedVolunteerField, Mode=TwoWay}"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          SelectionChanged="OnFieldChanged"/>



        <ListView Grid.Row="1"
          Margin="5"
          ItemsSource="{Binding Path=CourseList}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Column 1" DisplayMemberBinding="{Binding Field1}" />
                    <GridViewColumn Header="Column 2" DisplayMemberBinding="{Binding Field2}" />
                </GridView>
            </ListView.View>
        </ListView>


        <!-- כפתור הוספה -->
        <Button Grid.Row="2"
                Content="Add"
                HorizontalAlignment="Center"
                Height="50"
                Width="100" />
    </Grid>
</Window>
