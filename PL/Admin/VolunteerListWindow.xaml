<Window x:Class="PL.Admin.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="VolunteerListWindow" Height="3130" Width="3590"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <!--<Window.Resources>
         הגדרת מקור נתונים עבור המתנדבים 
        <local:VolunteerCollection x:Key="VolunteerCollectionKey" />
    </Window.Resources>-->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*" />
            <RowDefinition Height="500*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ComboBox להבחנה בין המתנדבים -->
        <ComboBox Grid.Row="0" 
          SelectedItem="{Binding SelectedCallType, Mode=TwoWay}" 
          ItemsSource="{Binding CallTypes}" 
          HorizontalAlignment="Stretch" Margin="10"/>


        <!-- ListView להצגת המתנדבים -->
        <ListView Grid.Row="1" 
                  Margin="10,1,10,10" 
                  ItemsSource="{Binding VolunteerList}" SelectedItem="{Binding SelectedVolunteer}" MouseDoubleClick="lsvVolunteerList_MouseDoubleClick" >
            <ListView.View>
                <GridView>

                    <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Id}" Width="100"/>
                    <!-- עמודת שם המתנדב -->
                    <GridViewColumn Header="FullName" DisplayMemberBinding="{Binding FullName}" Width="100"/>

                    <!-- עמודת מצב המתנדב -->
                    <GridViewColumn Header="Is Active" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive}" IsEnabled="False" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="SumTreatedCalls" DisplayMemberBinding="{Binding SumTreatedCalls}" Width="100"/>
                    <GridViewColumn Header="SumCanceledCalls" DisplayMemberBinding="{Binding SumCanceledCalls}" Width="100"/>
                    <GridViewColumn Header="SumExpiredCalls" DisplayMemberBinding="{Binding SumExpiredCalls}" Width="100"/>

                    <GridViewColumn Header="CallIdInTreatment" DisplayMemberBinding="{Binding CallIdInTreatment}" Width="100" />
                </GridView>
            </ListView.View>
        </ListView>

        <!-- כפתור הוספה -->
        <Button Grid.Row="1" 
        Content="Add" 
        HorizontalAlignment="Left" 
        Height="300" 
        Width="700" 
        Command="{Binding AddVolunteerCommand}" 
        Margin="10,10,0,10" 
        Click="ButtonAdd_Click" />

    </Grid>
</Window>
