<Window x:Class="PL.Admin.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Volunteer Management" 
        Height="800" 
        Width="1000"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Closed="Window_Closed"
        Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#D32F2F"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filters and Sorting -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <!-- Filter by Call Type -->
            <ComboBox Name="callTypeFilter" Width="150" Height="35" Margin="5"
                      ItemsSource="{Binding CallTypeOptions}"
                      SelectedItem="{Binding SelectedCallType}"
                      SelectionChanged="CallTypeFilter_SelectionChanged"
                      ToolTip="Filter by Call Type" />

            <!-- Filter by Active/Inactive -->
            <ComboBox Name="isActiveFilter" Width="150" Height="35" Margin="5"
                      ItemsSource="{Binding ActiveFilterOptions}"
                      SelectedItem="{Binding SelectedActiveFilter}"
                      SelectionChanged="IsActiveFilter_SelectionChanged"
                      ToolTip="Filter by Active Status" />

            <!-- Sort by Field -->
            <ComboBox Name="sortField" Width="150" Height="35" Margin="5"
                      ItemsSource="{Binding SortOptions}"
                      SelectedItem="{Binding SelectedSortOption}"
                      SelectionChanged="SortField_SelectionChanged"
                      ToolTip="Sort By" />
        </StackPanel>

        <!-- Volunteers ListView -->
        <ListView Grid.Row="1"
                  Margin="0,15"
                  BorderThickness="1"
                  BorderBrush="#FFCDD2"
                  ItemsSource="{Binding Volunteers}"
                  SelectedItem="{Binding SelectedVolunteer}"
                  MouseDoubleClick="lsvVolunteerList_MouseDoubleClick"
                  SelectionChanged="ListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="80"/>
                    <GridViewColumn Header="Full Name" DisplayMemberBinding="{Binding FullName}" Width="180"/>
                    <GridViewColumn Header="Status" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Active}" IsEnabled="False" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Total Calls" DisplayMemberBinding="{Binding SumTreatedCalls}" Width="100"/>
                    <GridViewColumn Header="Canceled" DisplayMemberBinding="{Binding SumCanceledCalls}" Width="100"/>
                    <GridViewColumn Header="Expired" DisplayMemberBinding="{Binding SumExpiredCalls}" Width="100"/>
                    <GridViewColumn Header="Current Call ID" DisplayMemberBinding="{Binding CallIdInTreatment}" Width="120"/>
                    <GridViewColumn Header="Current Call Type" DisplayMemberBinding="{Binding CallType}" Width="120"/>
                    <GridViewColumn Header="Actions" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete" Click="DeleteButton_Click" Tag="{Binding Id}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Add Button -->
        <Button Grid.Row="2"
                Content="Add New Volunteer"
                HorizontalAlignment="Center"
                Width="200"
                Height="40"
                Click="ButtonAdd_Click"
                FontWeight="SemiBold"
                FontSize="14">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="20"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
