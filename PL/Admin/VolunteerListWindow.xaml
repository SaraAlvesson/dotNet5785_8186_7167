<Window x:Class="PL.Admin.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="VolunteerListWindow" Height="3130" Width="3590"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*" />
            <RowDefinition Height="500*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ComboBox להבחנה בין המתנדבים -->
        <ComboBox Grid.Row="0" 
                  SelectedValue="{Binding SelectedCallTypeEnumField, Mode=TwoWay}" 
                  DisplayMemberPath="Name"
                  ItemsSource="{Binding CallTypes}" 
                  HorizontalAlignment="Stretch" Margin="10"/>

        <!-- ListView להצגת המתנדבים -->
        <ListView Grid.Row="1" 
                  Margin="5,11,5,65" 
                  ItemsSource="{Binding VolunteerList}" SelectionChanged="ListView_SelectionChanged" Grid.RowSpan="2">
            <ListView.View>
                <GridView>
                    <!-- עמודת שם המתנדב -->
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="Auto"/>

                    <!-- עמודת גיל המתנדב -->
                    <GridViewColumn Header="Age" DisplayMemberBinding="{Binding Age}" Width="Auto"/>

                    <!-- עמודת מצב המתנדב -->
                    <GridViewColumn Header="Is Active">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive}" IsEnabled="False"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- עמודת תכונה נוספת אם יש -->
                    <GridViewColumn Header="Other Property">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding OtherProperty}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- כפתור הוספה -->
        <Button Grid.Row="2" 
                Content="Add" 
                HorizontalAlignment="Center" 
                Height="50" 
                Width="100" 
                Command="{Binding AddVolunteerCommand}" 
                Margin="10" />
    </Grid>
</Window>
