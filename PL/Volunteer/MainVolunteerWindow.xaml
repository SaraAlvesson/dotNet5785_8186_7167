<Window x:Class="PL.Volunteer.MainVolunteerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="Volunteer Screen" Height="600" Width="800" DataContext="{Binding RelativeSource={RelativeSource Self}}">
    
    <Grid Margin="10">
        <StackPanel>
            <GroupBox Header="Volunteer Details" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="ID:" FontWeight="Bold" />
                    <TextBox Text="{Binding VolunteerId}" IsReadOnly="True" />
                    <TextBlock Text="Name:" FontWeight="Bold" />
                    <TextBox Text="{Binding Name}" />
                    <TextBlock Text="Address:" FontWeight="Bold" />
                    <TextBox Text="{Binding Address}" />
                    <TextBlock Text="Address:" FontWeight="Bold" />
                    <TextBox Text="{Binding Address}" />
                    <TextBlock Text="Address:" FontWeight="Bold" />
                    <TextBox Text="{Binding Address}" />
                    <Button Content="Update Details" Command="{Binding UpdateCommand}" />
                    
                    
                </StackPanel>
            </GroupBox>
            <GroupBox Header="Current Call">
                <StackPanel>
                    <TextBlock Text="Call ID:" FontWeight="Bold" />
                    <TextBox Text="{Binding CurrentCallId}" IsReadOnly="True" />
                    <TextBlock Text="Status:" FontWeight="Bold" />
                    <TextBox Text="{Binding CallStatus}" IsReadOnly="True" />
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Complete Call" Command="{Binding CompleteCallCommand}" Margin="0,0,10,0" Click="Button_Click" />
                        <Button Content="Cancel Call" Command="{Binding CancelCallCommand}" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</Window>
