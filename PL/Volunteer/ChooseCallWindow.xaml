<Window x:Class="PL.Volunteer.ChooseCallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Call for Handling" Height="600" Width="900" DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="Window_Loaded"
        Closed="Window_Closed">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filter and Update Address -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Filter by Call Type:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox Name="CallTypeFilter" Width="200" Margin="0,0,20,0"
                ItemsSource="{Binding CallTypes}" 
                SelectedItem="{Binding SelectedCallType, Mode=TwoWay}" 
                SelectionChanged="CallTypeFilter_SelectionChanged"/>

            <Label Content="Sort by:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox Name="SortByComboBox" Width="200" Margin="0,0,20,0"
                ItemsSource="{Binding SortOptions}" 
                SelectedItem="{Binding SelectedSortOption, Mode=TwoWay}" 
                SelectionChanged="SortByComboBox_SelectionChanged"/>

            <!-- TextBox for updating volunteer location -->
            <TextBox x:Name="textrr" 
                     Text="{Binding CurrentVolunteer.Location, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Top" Width="94" Margin="10" Height="29"  />

            <Button Content="Update address" Width="100" Height="35" Click="update_click" Background="#3498DB" Foreground="White" BorderBrush="#2980B9" />
        </StackPanel>

        <!-- Calls DataGrid -->
        <DataGrid x:Name="CallsDataGrid" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  CanUserSortColumns="True" 
                  Margin="0,10,0,10" 
                  AlternatingRowBackground="#F9F9F9" 
                  VerticalScrollBarVisibility="Auto" 
                  ItemsSource="{Binding Calls}" 
                  SelectionChanged="CallsDataGrid_SelectionChanged_1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="100"/>
                <DataGridTextColumn Header="Call Type" Binding="{Binding CallType}" Width="150"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="200"/>
                <DataGridTextColumn Header="Open Time" Binding="{Binding OpenTime}" Width="150"/>
                <DataGridTextColumn Header="Distance (km)" Binding="{Binding DistanceOfCall}" Width="100"/>
                <DataGridTextColumn Header="Max Finish Time" Binding="{Binding MaxFinishTime}" Width="150"/>

                <DataGridTemplateColumn Header="Action">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Select" Click="SelectButton_Click" 
                    CommandParameter="{Binding}" Width="80" Height="30" Background="#4CAF50" 
                    Foreground="White" FontWeight="Bold" BorderBrush="#4CAF50" Cursor="Hand"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

        <!-- Map and Call Details -->
        <Grid Grid.Row="2" Margin="0,20,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Map Section -->
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="10">
                <WebBrowser HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </Border>

            <!-- Call Details Section -->
            <StackPanel Grid.Column="1" Margin="10">
                <TextBlock Text="Call Details" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                <TextBox TextWrapping="Wrap" IsReadOnly="True" VerticalScrollBarVisibility="Auto" Height="69" Margin="0,0,0,10" 
                         Text="{Binding SelectedCallDetails}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
