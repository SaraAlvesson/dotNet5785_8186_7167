<Window x:Class="PL.MainWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="600" 
        Width="900" 
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="MainWindow_Loaded" 
        Closed="MainWindow_Closed"
        Background="#F0F3F4">
    <Window.Resources>
        <Style x:Key="RedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#D32F2F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#B71C1C"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" 
                                CornerRadius="5">
                            <TextBlock Text="{TemplateBinding Content}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontWeight="{TemplateBinding FontWeight}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B71C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TimeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#D32F2F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#B71C1C"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" 
                                CornerRadius="5">
                            <TextBlock Text="{TemplateBinding Content}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontWeight="{TemplateBinding FontWeight}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B71C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Time Display -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
            <Label Content="Current Time:" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Foreground="#2C3E50" 
                   HorizontalAlignment="Center"/>
            <Label Content="{Binding CurrentTime}" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="#E74C3C" 
                   HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Database Operation Buttons -->
        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Grid.Row="1" 
                    VerticalAlignment="Center">
            <Button Content="Initialize DB" 
                    Width="120" 
                    Height="40" 
                    Click="btnInit" 
                    Style="{StaticResource RedButtonStyle}"/>
            <Button Content="Reset DB" 
                    Width="120" 
                    Height="40" 
                    Click="btnReset" 
                    Style="{StaticResource RedButtonStyle}"/>
            <Button Content="Handle Volunteers" 
                    Width="120" 
                    Height="40" 
                    Click="btnVolunteerList" 
                    Style="{StaticResource RedButtonStyle}"/>
            <Button Content="Handle Calls" 
                    Width="120" 
                    Height="40" 
                    Click="btnHandleCalls" 
                    Style="{StaticResource RedButtonStyle}"/>
        </StackPanel>

        <!-- Time Manipulation Buttons -->
        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Grid.Row="2" 
                    Margin="0,20,0,0">
            <Button Content="קידום בדקה" 
                    Width="110" 
                    Height="40" 
                    Click="btnAddMin" 
                    Style="{StaticResource TimeButtonStyle}"/>
            <Button Content="קידום בשעה" 
                    Width="110" 
                    Height="40" 
                    Click="btnAddHour" 
                    Style="{StaticResource TimeButtonStyle}"/>
            <Button Content="קידום ביום" 
                    Width="110" 
                    Height="40" 
                    Click="btnAddDay" 
                    Style="{StaticResource TimeButtonStyle}"/>
            <Button Content="קידום בחודש" 
                    Width="110" 
                    Height="40" 
                    Click="btnAddMonth" 
                    Style="{StaticResource TimeButtonStyle}"/>
            <Button Content="קידום בשנה" 
                    Width="110" 
                    Height="40" 
                    Click="btnAddYear" 
                    Style="{StaticResource TimeButtonStyle}"/>
        </StackPanel>

        <!-- RiskRange Update Section -->
        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Grid.Row="3" 
                    Margin="0,30,0,0">
            <TextBox Name="textrr" 
                     Width="150" 
                     Height="35" 
                     Text="{Binding RiskRange, UpdateSourceTrigger=PropertyChanged}" 
                     Background="#ECF0F1" 
                     BorderBrush="#E74C3C" 
                     Foreground="#2C3E50" 
                     Margin="0,0,10,0" 
                     TextChanged="textrr_TextChanged"/>
            <Button Content="Update" 
                    Width="100" 
                    Height="35" 
                    Click="update_click" 
                    Style="{StaticResource RedButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>