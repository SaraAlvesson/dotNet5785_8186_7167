<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="900"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Grid Margin="15" Background="#F4F4F4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Label for displaying the current system time -->
        <Label Content="Current Time:" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center" Foreground="#2C3E50" Grid.Row="0" />
        <Label Content="{Binding CurrentTime}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#34495E" Grid.Row="0" Margin="0,30,0,0" />

        <!-- Centered buttons for database operations -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1" VerticalAlignment="Center">
            <Button Content="Initialize DB" Width="120" Height="40" Click="btnInit" Background="#BDC3C7" Foreground="#2C3E50" BorderBrush="#7F8C8D" FontWeight="SemiBold" />
            <Button Content="Reset DB" Width="120" Height="40" Click="btnReset" Background="#BDC3C7" Foreground="#2C3E50" BorderBrush="#7F8C8D" FontWeight="SemiBold" />
            <Button Content="Handle Volunteers" Width="120" Height="40" Click="btnVolunteerList" Background="#BDC3C7" Foreground="#2C3E50" BorderBrush="#7F8C8D" FontWeight="SemiBold" />
            <Button Content="Handle Calls" Width="120" Height="40" Click="btnHandleCalls" Background="#BDC3C7" Foreground="#2C3E50" BorderBrush="#7F8C8D" FontWeight="SemiBold" />
        </StackPanel>

        <!-- Buttons for time manipulation -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="2" Margin="0,20,0,0">
            <Button Content="קידום בדקה" Width="110" Height="40" Click="btnAddMin" Background="#EAEDED" Foreground="#2C3E50" BorderBrush="#95A5A6" />
            <Button Content="קידום בשעה" Width="110" Height="40" Click="btnAddHour" Background="#EAEDED" Foreground="#2C3E50" BorderBrush="#95A5A6" />
            <Button Content="קידום ביום" Width="110" Height="40" Click="btnAddDay" Background="#EAEDED" Foreground="#2C3E50" BorderBrush="#95A5A6" />
            <Button Content="קידום בחודש" Width="110" Height="40" Click="btnAddMonth" Background="#EAEDED" Foreground="#2C3E50" BorderBrush="#95A5A6" />
            <Button Content="קידום בשנה" Width="110" Height="40" Click="btnAddYear" Background="#EAEDED" Foreground="#2C3E50" BorderBrush="#95A5A6" />
        </StackPanel>

        <!-- TextBox for RiskRange and update button -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="3" Margin="0,30,0,0">
            <TextBox Name="textrr" Width="150" Height="35" Text="{Binding RiskRange, UpdateSourceTrigger=PropertyChanged}" Background="#ECF0F1" BorderBrush="#BDC3C7" Foreground="#2C3E50" Margin="0,0,10,0" TextChanged="textrr_TextChanged" />
            <Button Content="Update" Width="100" Height="35" Click="update_click" Background="#3498DB" Foreground="White" BorderBrush="#2980B9" />
        </StackPanel>
    </Grid>
</Window>
